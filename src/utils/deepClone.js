export function deepClone(target) {
  // for (let i = 0, len = target.length; i < len; i++) {
  //   console.log(target[i].path, pathname);
  //   if (target[i].path === pathname) {
  //     return target[i];
  //   }
  //   if (Array.isArray(target[i].children)) {
  //     deepClone(target[i].children, pathname);
  //   }
  // }
}
